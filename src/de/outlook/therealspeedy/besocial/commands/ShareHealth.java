package de.outlook.therealspeedy.besocial.commands;

import org.bukkit.command.Command;
import org.bukkit.command.CommandExecutor;
import org.bukkit.command.CommandSender;

public class ShareHealth implements CommandExecutor {

    @Override
    public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
        //TODO: implement sharehealth: send target half of senders hearts, except when target has not enough space
        return false;
    }
}
